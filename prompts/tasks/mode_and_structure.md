# Task: mode_and_structure

Choose the edit mode and propose the structural order.

## Canonical references
- 01_Project_Instructions_V3_2_Efficient.md — §7 Mode selection, §8 Structure
- 02_Kickoff_Intent_and_Presets_V3_2_Efficient.md — presets and inference defaults

## Task instruction
- Pick one mode: narrative | selects | cleanup. Respect settled decisions;
  infer from setup + presets where unset.
- `rationale`: 2–3 sentences, concrete, referencing the material.
- `proposed_order`: content-function names or segment groupings in intended
  running order (e.g. value-first opening, strongest resolved close where the
  preset calls for it).
- `exclusions`: what is deliberately left out and why (short phrases).

## Context provided
Structured project state relevant to this task is appended as JSON.

## Output
A single JSON object valid against the appended schema. No prose.
