{
  "provider": "val",
  "model": null,
  "max_context_tokens": 16000,
  "max_output_tokens": 2000,
  "supports_system_prompt": true,
  "supports_json_schema_mode": false,
  "json_strategy": "prompt_and_repair",
  "chunking": { "transcript_chunk_tokens": 6000, "overlap_tokens": 300 },
  "retry": { "max_attempts": 2, "repair_prompt": true },
  "endpoint": {
    "base_url": null,
    "api_style": null,
    "auth_env": "VAL_API_KEY",
    "auth_header": "Authorization",
    "auth_scheme": "Bearer",
    "timeout_seconds": 120.0,
    "transport_retries": 1
  },
  "_wiring": "To connect VAL: (1) set base_url (gateway URL, no trailing endpoint path), (2) set api_style to 'openai_chat' or 'anthropic_messages' to match the gateway, (3) set model to the VAL model id, (4) export VAL_API_KEY on the SAGE server (never in this file, never from users), (5) confirm via GET /api/admin/status in an admin deployment. Adjust auth_header/auth_scheme only if the gateway uses a non-standard header (e.g. 'x-api-key' with auth_scheme null). Update max_context/output once real limits are confirmed.",
  "_env_overrides": "base_url, api_style, and model above may each be overridden at runtime by the env vars VAL_BASE_URL, VAL_API_STYLE, VAL_MODEL (set in the hosting dashboard) — useful for pointing a staging deployment at a different VAL model/gateway without editing this file. VAL_API_KEY is always an env var, never a file value."
}
